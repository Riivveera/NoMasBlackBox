# NoMasBlackCaja
A project investigating if common image data augmentation techniques (like cropping, flipping, etc.) make CNN models like VGG16 and ResNet more interpretable. Using CIFAR-100 and XAI methods (like Grad-CAM), we'll visualize where the model "looks" to see if augmentation leads to more focused and sensible decision-making, or just higher accuracy.
